import { MybannercssDirective } from './mybannercss.directive';

describe('MybannercssDirective', () => {
  it('should create an instance', () => {
    const directive = new MybannercssDirective();
    expect(directive).toBeTruthy();
  });
});
